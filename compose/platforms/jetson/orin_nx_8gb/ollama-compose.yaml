version: "3"

services:
  ollama:
    image: ollama/ollama:latest
    container_name: fusionloom-ollama
    volumes:
      - ${OLLAMA_VOLUME}:/root/.ollama
    ports:
      - "11434:11434"
    environment:
      - OLLAMA_HOST=0.0.0.0
      # Jetson Orin NX 8GB optimizations
      - OLLAMA_CPU_ONLY=0
      # Optimized resources for Orin NX 8GB
      - OLLAMA_NUM_THREADS=8
      - OLLAMA_MAX_MEMORY=6G
    restart: unless-stopped
    labels:
      - "io.containers.autoupdate=image"
    networks:
      - fusionloom_net
    # Add NVIDIA GPU support
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
        limits:
          memory: 7G
    platform: linux/arm64  # Specify ARM64 platform for Jetson

networks:
  fusionloom_net:
    external: true
